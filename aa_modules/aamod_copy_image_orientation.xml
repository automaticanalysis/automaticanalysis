<?xml version="1.0" encoding="utf-8"?>
<!-- Takes realignment parameters from one stream and applies them to another -->
<!-- There must be the same number of images in each of the two input streams -->
<aap>
    <tasklist>
        <currenttask domain='session' desc='Apply realignment parameters from one stream to another' modality='MRI'>
            <permanenceofoutput>1</permanenceofoutput>
            
            ﻿<inputstreams>
                <stream>epi</stream>  
                <stream>epi_nulled</stream>  
            </inputstreams>
            
            <outputstreams>
                <stream>epi_nulled</stream>
            </outputstreams>
            
        </currenttask>
    </tasklist>
</aap>
