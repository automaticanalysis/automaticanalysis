<?xml version="1.0" encoding="utf-8"?>
<aap>
    <tasklist>
        <currenttask mfile_alias='aamod_coreg_noss' domain='subject' desc='SPM coregistration' modality='MRI'>
            
            <qsub>
                <timeBase>0.05</timeBase>
                <memoryBase>0.2</memoryBase>
            </qsub>
            
            <permanenceofoutput>1</permanenceofoutput>
            
            <!-- NB. The order of the streams is important; the first stream
            name is used as the input stream (e.g., 'structural' or 't1'). -->
            
            <inputstreams>
                <stream>t1</stream>  
                <stream>meanepi</stream>                  
            </inputstreams>
            
            <outputstreams>
                <stream>t1</stream>
            </outputstreams>
            
        </currenttask>
    </tasklist>
</aap>

